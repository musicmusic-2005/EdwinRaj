pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code checkout stage'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage running'
            }
        }
    }
}
